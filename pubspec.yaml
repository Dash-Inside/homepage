name: homepage
description: dash inside homepage
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  dartz: ^0.10.1
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.1
  lorem_ipsum: ^0.0.3
  responsive_builder: ^0.7.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  freezed: ^2.4.2

flutter:
  uses-material-design: true
  assets:
    - assets/
